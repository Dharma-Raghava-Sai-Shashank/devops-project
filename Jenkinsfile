pipeline {
    agent any

    stages {

        stage('Test') {
            steps {
                echo 'Running tests'
            }
        }

        stage('Build') {
            steps {
                echo 'Building'
            }
        }
    }
}